/**
 * @param {number[]} nums
 * @return {number}
 */
// TODO:
var maxProduct = function(nums) {
	
	let max = 1
	let min = 1
	let flag = false
	let max_so_far = 1

	for(let i =0 ; i < nums.length; i++){

		// if(arr[i]>0){
		// 	max =
		// }
	}

	if(flag === false && max_so_far ===1){
		return 0
	}
	return max_so_far
};

let arr1 = [2,3,-2,4]

let arr2 = [-2,0,-1]